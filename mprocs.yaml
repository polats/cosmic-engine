procs:
  client:
    cwd: packages/uri-server
    shell: yarn dev
  hardhat-chain:
    cwd: packages/contracts
    shell: yarn chain
  test-contracts:
    cwd: packages/contracts
    shell: forge test
  deploy-contracts:
    cwd: packages/contracts
    shell: yarn deploy
skipped:
  anvil:
    cwd: packages/contracts
    shell: anvil --block-time 2 --base-fee 0

