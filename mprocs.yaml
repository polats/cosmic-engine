procs:
  client:
    cwd: packages/nextjs
    shell: yarn dev
  contracts:
    cwd: packages/contracts
    shell: forge test
  anvil:
    cwd: packages/contracts
    shell: anvil --block-time 2 --base-fee 0
